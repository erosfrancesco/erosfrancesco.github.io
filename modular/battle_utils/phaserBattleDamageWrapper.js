import BattleDamage  from './battle-damage.js';

export default PhaserDamage() {
    constructor(options) {
        const {value, types, Sprite} = options;
        types.value = value;
        super(types);
        this.Sprite = Sprite;
    }
    
    display() {
        // new Phaser text
        // compute x, y
        // display
    }
}
