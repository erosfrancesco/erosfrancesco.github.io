// import modules (es6)
import Character from './character.js';

export default Character;

/*
let dummy = new Character({
	name: 'xiod',
	activeStatuses: {
		'zombie': true
	}
});

console.log( 

	dummy.isAlly(), 
	dummy.life,
	dummy.mana,
	dummy.getLevel(),
	dummy.getVelocity(),
	dummy.getStrenght(),
	dummy.Statuses

);
/**/